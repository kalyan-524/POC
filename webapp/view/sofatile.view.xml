<mvc:View
    controllerName="ladera.royalenfield.controller.sofatile"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:u="sap.ui.unified"
    xmlns:table="sap.ui.table">

<Page id="Sofatile" showHeader="false">

   
    <Toolbar>
        <Button text="Back" icon="sap-icon://nav-back" press="onnavback"/>
    </Toolbar>

    <content>
        <table:TreeTable
            id="TreeTableBasic"
            rows="{path:'/catalog/store', parameters: {arrayNames:['categories']}}"
            selectionMode="MultiToggle"
            enableSelectAll="false"
            ariaLabelledBy="title">

           
            <table:extension>
                <!-- <OverflowToolbar>
                    <Title id="title" text="Clothing"/>
                    <ToolbarSpacer/>
                    <Button text="Collapse all" press="onCollapseAll"/>
                    <Button text="Collapse selection" press="onCollapseSelection"/>
                    <Button text="Expand first level" press="onExpandFirstLevel"/>
                    <Button text="Expand selection" press="onExpandSelection"/>
                </OverflowToolbar> -->
            </table:extension>

            
            <table:columns>
                <table:Column>
                    <Label text="Category"/>
                    <table:template>
                        <Text text="{name}"/>
                    </table:template>
                </table:Column>

                <table:Column width="9rem">
                    <Label text="Price"/>
                    <table:template>
                        <u:Currency value="{amount}" currency="{currency}"/>
                    </table:template>
                </table:Column>

                <table:Column width="11rem">
                    <Label text="Quantity"/>
                    <table:template>
                        <Select
                            items="{path: '/sizes', templateShareable: true}"
                            selectedKey="{size}"
                            visible="{= !!${size}}"
                            forceSelection="false">
                            <core:Item key="{key}" text="{value}"/>
                        </Select>
                    </table:template>
                </table:Column>
            </table:columns>

        </table:TreeTable>
    </content>
</Page>
</mvc:View>